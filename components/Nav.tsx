import Link from 'next/link';
import { signOut } from '@/lib/auth';
export function Nav({ budgetYearId }: { budgetYearId?: string }) { return <header className="border-b bg-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-4"><Link href="/" className="font-semibold">Family Budget Pro</Link><nav className="flex items-center gap-4 text-sm text-slate-600"><Link href="/">Dashboard</Link>{budgetYearId ? <><Link href={`/budget/${budgetYearId}`}>Budget</Link><Link href={`/transactions/${budgetYearId}`}>Transactions</Link><Link href={`/reports/${budgetYearId}`}>Reports</Link><Link href={`/settings/${budgetYearId}`}>Settings</Link></> : null}<form action={async()=>{'use server'; await signOut({ redirectTo: '/login' });}}><button className="rounded-lg border px-3 py-1.5">Sign out</button></form></nav></div></header>; }
