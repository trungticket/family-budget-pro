use client';
import { Bar, BarChart, CartesianGrid, Legend, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis, PieChart, Pie, Cell } from 'recharts';
type MonthlyDatum = { month:string; income:number; expenses:number; savings:number; spendingBalance:number; savingsBalance:number };
export function IncomeExpenseChart({ data }: { data: MonthlyDatum[] }) { return <div className="h-80 rounded-2xl border bg-white p-5 shadow-sm"><h3 className="mb-4 font-semibold">Income / Expenses / Savings</h3><ResponsiveContainer width="100%" height="85%"><BarChart data={data}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="month"/><YAxis/><Tooltip/><Legend/><Bar dataKey="income" name="Income" fill="#0f766e"/><Bar dataKey="expenses" name="Expenses" fill="#b91c1c"/><Bar dataKey="savings" name="Savings" fill="#2563eb"/></BarChart></ResponsiveContainer></div>; }
export function BalanceChart({ data }: { data: MonthlyDatum[] }) { return <div className="h-80 rounded-2xl border bg-white p-5 shadow-sm"><h3 className="mb-4 font-semibold">Spending and Savings Balance</h3><ResponsiveContainer width="100%" height="85%"><LineChart data={data}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="month"/><YAxis/><Tooltip/><Legend/><Line type="monotone" dataKey="spendingBalance" name="Spending Balance" stroke="#334155" strokeWidth={2}/><Line type="monotone" dataKey="savingsBalance" name="Savings Balance" stroke="#2563eb" strokeWidth={2}/></LineChart></ResponsiveContainer></div>; }
const palette=['#0f766e','#2563eb','#7c3aed','#db2777','#ea580c','#475569','#15803d','#9333ea','#0369a1','#be123c'];
export function ExpensePie({ data }: { data: { name:string; value:number }[] }) { return <div className="h-80 rounded-2xl border bg-white p-5 shadow-sm"><h3 className="mb-4 font-semibold">Expense Mix by Group</h3><ResponsiveContainer width="100%" height="85%"><PieChart><Tooltip/><Legend/><Pie data={data} dataKey="value" nameKey="name" innerRadius={55} outerRadius={100} paddingAngle={2}>{data.map((_,i)=><Cell key={i} fill={palette[i%palette.length]}/>)}</Pie></PieChart></ResponsiveContainer></div>; }
