import { addCategory, archiveCategory, updateBudgetSettings } from '@/app/actions';
import { Nav } from '@/components/Nav';
import { requireBudgetAccess } from '@/lib/access';
import { loadBudgetData } from '@/lib/budget-data';
export default async function SettingsPage({ params }: { params: Promise<{ budgetYearId: string }> }) { const { budgetYearId } = await params; await requireBudgetAccess(budgetYearId); const { budgetYear, groups } = await loadBudgetData(budgetYearId); return <><Nav budgetYearId={budgetYearId}/><main className="mx-auto max-w-7xl px-6 py-8"><h1 className="text-3xl font-semibold tracking-tight">Settings</h1><section className="mt-8 grid gap-6 lg:grid-cols-[380px_1fr]"><form action={updateBudgetSettings} className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="font-semibold">Budget year settings</h2><input type="hidden" name="budgetYearId" value={budgetYearId}/><label className="mt-4 block text-sm">Year<input name="year" type="number" defaultValue={budgetYear.year} className="mt-1 w-full rounded-lg border px-3 py-2"/></label><label className="mt-3 block text-sm">Currency<input name="currency" defaultValue={budgetYear.currency} className="mt-1 w-full rounded-lg border px-3 py-2"/></label><label className="mt-3 block text-sm">Beginning spending balance<input name="beginningSpendingBalance" type="number" defaultValue={Number(budgetYear.beginningSpendingBalance)} className="mt-1 w-full rounded-lg border px-3 py-2"/></label><label className="mt-3 block text-sm">Beginning savings balance<input name="beginningSavingsBalance" type="number" defaultValue={Number(budgetYear.beginningSavingsBalance)} className="mt-1 w-full rounded-lg border px-3 py-2"/></label><button className="mt-4 w-full rounded-lg bg-slate-950 px-4 py-2 font-medium text-white">Save settings</button><div className="mt-6 border-t pt-4"><p className="text-sm text-slate-500">Import Excel vào route API:</p><code className="mt-2 block rounded bg-slate-100 p-2 text-xs">POST /api/import/{budgetYearId}</code></div></form><div className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="font-semibold">Categories</h2><div className="mt-4 space-y-6">{groups.map(g=><div key={g.id} className="rounded-xl border p-4"><div className="flex items-center justify-between"><div><div className="font-semibold">{g.name}</div><div className="text-xs text-slate-500">{g.type}</div></div><form action={addCategory} className="flex gap-2"><input type="hidden" name="budgetYearId" value={budgetYearId}/><input type="hidden" name="categoryGroupId" value={g.id}/><input name="name" placeholder="New category" className="rounded-lg border px-3 py-2 text-sm"/><button className="rounded-lg border px-3 py-2 text-sm">Add</button></form></div><div className="mt-4 flex flex-wrap gap-2">{g.categories.filter(c=>!c.archived).map(c=><form key={c.id} action={archiveCategory}><input type="hidden" name="budgetYearId" value={budgetYearId}/><input type="hidden" name="categoryId" value={c.id}/><button className="rounded-full border px-3 py-1 text-xs hover:bg-red-50">{c.name} ×</button></form>)}</div></div>)}</div></div></section></main></> }
